# === VERCEL ENVIRONMENT VARIABLES ===
# Configurar en Vercel dashboard → Settings → Environment Variables

# URL pública del backend en Railway + prefijo /api/v1 (sin trailing slash)
VITE_API_URL=https://inari-backend.up.railway.app/api/v1
